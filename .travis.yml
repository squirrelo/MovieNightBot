language: python
python:
  - "3.7"
# command to install dependencies
install:
  - pip install .[test]
# command to run tests
script:
  - flake8 tests setup.py movienightbot
  - black --check tests setup.py movienightbot
  - pytest
